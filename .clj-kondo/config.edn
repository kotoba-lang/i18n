;; defmessages (i18n.messages) reads an EDN resource file at macroexpansion
;; time and defs one fn per message key; clj-kondo can't see those without
;; evaluating the resource, so the generated symbols the test exercises are
;; excluded here rather than statically resolved.
{:linters {:unresolved-symbol {:exclude [app-title actions-save auth-welcome todos-count]}}}
